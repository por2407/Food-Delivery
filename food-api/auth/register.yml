info:
  name: register
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/api/register
  body:
    type: json
    data: |-
      {
        "name": "จอป กูด",
        "email": "job@gmail.com",
        "password": "job123456789",
        "phone": "0894561112",
        "role": "rest"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
