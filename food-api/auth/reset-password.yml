info:
  name: reset-password
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:3000/api/reset-password
  body:
    type: json
    data: |-
      {
        "email": "por@gmail.com",
        "phone": "0957712223",
        "new_password": "por123456789"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
